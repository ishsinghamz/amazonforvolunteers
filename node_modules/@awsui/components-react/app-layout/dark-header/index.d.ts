import React from 'react';
export interface DarkHeaderProps {
    isRefresh: boolean;
    isMobile: boolean;
    toolsWidth: number;
    navigationWidth: number;
    children?: React.ReactNode;
    topOffset?: number;
    sticky?: boolean;
}
export declare function DarkHeader({ isMobile, isRefresh, children, toolsWidth, navigationWidth, topOffset, sticky, }: DarkHeaderProps): JSX.Element;
