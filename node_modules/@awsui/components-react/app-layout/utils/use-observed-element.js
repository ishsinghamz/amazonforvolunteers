import { useResizeObserver } from '../../internal/hooks/container-queries';
import { useCallback } from 'react';
var extractHeight = function (rect) { return rect === null || rect === void 0 ? void 0 : rect.borderBoxSize[0].blockSize; };
export function useObservedElement(selectorOrRef) {
    var _a;
    var getElement = useCallback(function () {
        var _a;
        if (typeof selectorOrRef === 'string') {
            return document.querySelector(selectorOrRef);
        }
        else {
            return (_a = selectorOrRef === null || selectorOrRef === void 0 ? void 0 : selectorOrRef.current) !== null && _a !== void 0 ? _a : null;
        }
    }, [selectorOrRef]);
    return (_a = useResizeObserver(getElement, extractHeight)) !== null && _a !== void 0 ? _a : 0;
}
