import { __assign } from "tslib";
import React from 'react';
import clsx from 'clsx';
import useFocusVisible from '../../internal/hooks/focus-visible';
import Icon from '../../icon/internal';
import styles from './styles.css.js';
function TriggerButton(_a, ref) {
    var ariaLabel = _a.ariaLabel, iconName = _a.iconName, onClick = _a.onClick, _b = _a.selected, selected = _b === void 0 ? false : _b, className = _a.className;
    var focusVisible = useFocusVisible();
    return (React.createElement("button", __assign({ "aria-label": ariaLabel, "aria-expanded": false, className: clsx(styles.trigger, className), "data-selected": selected, onClick: onClick, type: "button", ref: ref }, focusVisible),
        React.createElement(Icon, { name: iconName })));
}
export default React.forwardRef(TriggerButton);
