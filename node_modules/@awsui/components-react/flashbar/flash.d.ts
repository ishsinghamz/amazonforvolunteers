import { FlashbarProps } from './interfaces';
interface Flash extends FlashbarProps.MessageDefinition {
    className: string;
}
export declare function Flash({ header, content, dismissible, dismissLabel, loading, action, buttonText, onButtonClick, onDismiss, className, type, }: Flash): JSX.Element;
export {};
