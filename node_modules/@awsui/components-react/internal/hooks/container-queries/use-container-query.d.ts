import React from 'react';
export interface Rectangle {
    width: number;
    height: number;
}
export declare function useContainerQuery<S>(mapFn: (rect: Rectangle, prev: S | null) => S, deps?: React.DependencyList): [S | null, React.MutableRefObject<any>];
