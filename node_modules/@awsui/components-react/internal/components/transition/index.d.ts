import React from 'react';
import { MutableRefObject } from 'react';
import { TransitionStatus as ReactTransitionGroupTransitionStatus } from 'react-transition-group';
export declare type TransitionStatus = ReactTransitionGroupTransitionStatus | 'enter' | 'exit';
export interface TransitionProps {
    in: boolean;
    exit?: boolean;
    disabled?: boolean;
    children: (state: ReactTransitionGroupTransitionStatus, transitioningElementRef: MutableRefObject<any>) => React.ReactNode;
    onStatusChange?: (status: TransitionStatus) => void;
}
export declare function Transition({ in: isIn, children, exit, onStatusChange, disabled, }: TransitionProps): JSX.Element;
