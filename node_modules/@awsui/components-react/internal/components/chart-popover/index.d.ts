import React from 'react';
import { PopoverProps } from '../../../popover/interfaces';
export interface ChartPopoverProps extends PopoverProps {
    title?: React.ReactNode;
    trackRef: React.RefObject<Element>;
    trackKey?: string | number;
    container: Element | null;
    onDismiss: (outsideClick?: boolean) => void;
    children?: React.ReactNode;
}
export default function ChartPopover({ position, size, fixedWidth, dismissButton, dismissAriaLabel, children, title, trackRef, trackKey, onDismiss, container, ...restProps }: ChartPopoverProps): JSX.Element;
