import { PopoverProps } from './internal';
export { PopoverProps };
export default function Popover({ position, size, fixedWidth, triggerType, dismissButton, renderWithPortal, ...rest }: PopoverProps): JSX.Element;
