import React from 'react';
interface WizardFormHeaderProps {
    children: React.ReactNode;
    isMobile: boolean;
    isVisualRefresh: boolean;
}
export default function WizardFormHeader({ children, isMobile, isVisualRefresh }: WizardFormHeaderProps): JSX.Element;
export {};
